name: spendwise
description: "Spendwise budget tracking app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.19.0
  cloud_firestore: ^6.6.0
  firebase_core: ^4.11.0
  firebase_auth: 6.5.4
  google_sign_in: ^6.0.0
  table_calendar: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
